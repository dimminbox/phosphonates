function visualtitle (limitt){
             var n = $('#FilmLanguage_title').val().length;
             $("#title").text("Введено " + n + " символов из "+limitt+" .");
};
function visualmeta (limitt){
            var n = $('#FilmLanguage_meta_description').val().length;
            $("#meta_description").text("Введено " + n + " символов "+limitt+" .");
  };